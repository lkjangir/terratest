package test

import (
	"fmt"
	"testing"
)

// Basic test to ensure we can successfully generate key pairs (no explicit validation for now)
func testMsg() {
	m := "Test function"
	fmt.Sprintf(m)
	return m
}
